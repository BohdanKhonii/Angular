import { AppShadowDirective } from './app-shadow.directive';

describe('AppShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new AppShadowDirective();
    expect(directive).toBeTruthy();
  });
});
